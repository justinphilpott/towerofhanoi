

/**
 * 
 */
export const resetTutPage = (context: any, event: Event) => {

  // increment the page counter for the tutorial pages, this way we can add 
  // as many as needed and change without needed to have specific names


}


